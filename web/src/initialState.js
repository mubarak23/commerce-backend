import { SportIdToName } from "./utils/core"

const initialState = {
  showLoading: false,
  showLoadingOverlay: false,
  // sidebarShow: 'responsive',
  sidebarShow: true,
  loggedIn: false,
}

export default initialState
